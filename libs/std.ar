import list;
import maybe;
import either;
import tuple;

let (+) = addInt;
let (-) = subInt;
let (*) = mulInt;
let (/) = divInt;

let (+.) = addFloat;
let (-.) = subFloat;
let (*.) = mulFloat;
let (/.) = divFloat;

